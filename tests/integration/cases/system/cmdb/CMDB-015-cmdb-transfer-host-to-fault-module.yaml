id: CMDB-015
name: cmdb transfer_host_to_fault_module happy path
tags:
  - coverage-system_go
  - suite-integration
steps:
  - name: init mock-backed context
    command:
      - context
      - init
      - --bk_api_url_tmpl
      - ${MOCK_API_BASE_URL}/{gateway_name}
    expect:
      exit_code: 0
      checks:
        - path: ok
          equals: true
  - name: login
    command:
      - auth
      - login
      - --access_token
      - integration-token
    expect:
      exit_code: 0
      checks:
        - path: ok
          equals: true
  - name: transfer host to fault module
    command:
      - cmdb
      - transfer_host_to_fault_module
      - --bk_biz_id
      - "2"
      - --bk_host_ids
      - 101,102
    expect:
      exit_code: 0
      checks:
        - path: ok
          equals: true
        - path: data.path
          contains: /bk-cmdb/prod/api/v3/open/hosts/modules/fault
        - path: data.body.bk_host_id.1
          equals: 102
